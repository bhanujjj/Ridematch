feast==0.40.1
pandas==2.2.2
pyarrow==14.0.2
numpy==1.26.4
redis==5.0.1
boto3>=1.42.0
botocore>=1.42.0
s3transfer>=0.16.0
mlflow==2.13.0  # pin to match the ghcr.io/mlflow/mlflow:v2.13.0 server image --
                # a newer client (3.x) calls a /logged-models endpoint the 2.13
                # server doesn't have, so log_model()/register_model() 404
setuptools<81  # mlflow==2.13.0 imports pkg_resources; setuptools>=81 dropped it entirely
prefect>=2.14.0,<3.0.0
scikit-learn
confluent-kafka
fastapi
uvicorn[standard]
prometheus-client
httpx